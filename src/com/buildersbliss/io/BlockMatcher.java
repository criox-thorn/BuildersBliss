package com.buildersbliss.io;

import com.google.gson.Gson;
import com.google.gson.JsonObject;

import java.util.ArrayList;
import java.io.File;
import java.io.FileReader;

public class BlockMatcher {
    public static ArrayList<String> match(double[] lab, String topDir) {
        Gson gson = new Gson();
        File extFolder = new File(topDir);

//        for (int mod = 0; mod < extFolder.)
//
//        JsonObject data = gson.fromJson(new FileReader(topDir, ))


        ArrayList<String> matches = new ArrayList<>();
        return matches;
    }
}
